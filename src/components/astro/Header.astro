---
import Search from "./Search.astro";

const navItems = [
  { href: "/", label: "홈" },
  { href: "/categories/development", label: "Development" },
  { href: "/categories/library", label: "Library" },
];

// 프로젝트 드롭다운 메뉴 (외부 링크로 리다이렉트)
// const projectItems = [
//   { href: "https://github.com/ywc0008/blog", label: "Blog", description: "이 블로그 소스코드" },
//   { href: "https://github.com/ywc0008", label: "GitHub", description: "GitHub 프로필" },
//   // 여기에 프로젝트 추가
// ];
---

<header class="sticky top-0 z-50 bg-white border-b border-gray-200 shadow-sm">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <a
        href="/"
        class="flex items-center gap-2 text-xl font-bold text-gray-900 no-underline hover:text-blue-600 transition-colors"
      >
        <span>ywc.life</span>
      </a>

      <div class="flex items-center gap-4">
        <nav>
          <ul class="flex items-center gap-6 list-none m-0 p-0">
            {
              navItems.map((item) => (
                <li>
                  <a
                    href={item.href}
                    class="text-gray-700 no-underline hover:text-blue-600 font-medium transition-colors"
                  >
                    {item.label}
                  </a>
                </li>
              ))
            }

            {/* Project 드롭다운 메뉴 */}
            <!-- <li class="relative group">
              <button
                class="flex items-center gap-1 text-gray-700 hover:text-blue-600 font-medium transition-colors cursor-pointer bg-transparent border-none"
              >
                Project
                <svg
                  class="w-4 h-4 transition-transform group-hover:rotate-180"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>

              {/* 드롭다운 메뉴 */}
              <div
                class="absolute right-0 top-full mt-2 w-56 bg-white border border-gray-200 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50"
              >
                <div class="py-2">
                  {
                    projectItems.map((project) => (
                      <a
                        href={project.href}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="block px-4 py-2 text-gray-700 no-underline hover:bg-gray-50 hover:text-blue-600 transition-colors"
                      >
                        <span class="font-medium">{project.label}</span>
                        {project.description && (
                          <span class="block text-sm text-gray-500">{project.description}</span>
                        )}
                      </a>
                    ))
                  }
                </div>
              </div>
            </li> -->
          </ul>
        </nav>
        <Search />
      </div>
    </div>
  </div>
</header>
